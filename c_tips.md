# C 语言技巧汇总

**记录和 小溪 聊天中学到的 C 语言实用技巧。用 VS Code 打开，配合 Markdown 预览更清晰。**

---

## 1. `scanf` 格式化技巧

**💡 核心规则**：`*` 在 `%` 后面表示 **"读取但不存储"**，跳过匹配的内容。

格式：`%*<类型>` → 读这个类型的数，然后扔掉。

---

### 1.1 `%*c` —— 跳过一个字符

```c
int a;
scanf("%d%*c", &a);  // 读取整数后跳过一个字符（如换行符）
```

常用于读取整数后跳过紧跟的换行符，避免影响下一次 `scanf`。

---

### 1.2 `%*s` —— 跳过一个字符串

```c
int age;
scanf("%*s%d", &age);  // 跳过开头的单词，只读取后面的数字
```

输入 `Age20` → `%*s` 跳过 `Age`，`%d` 读取 `20`。

---

### 1.3 `%[^...]` —— 读取指定字符集

```c
char name[50];
scanf("%[^\n]", name);  // 读取直到换行（相当于 gets，但更安全）
```

- `%[^0-9]` —— 读取直到遇到数字字符
- `%[a-z]` —— 只读取小写字母
- `%[^\n]` —— 读取一整行（不含换行符）

---

### 1.4 实战：解析 `80kg,1.7m` 格式

```c
int weight;
float height;
scanf("%*[^0-9]%d%*[^0-9.]%f", &weight, &height);
//               ^^^^^      ^^^^^^^^
//              跳过"kg,1"  跳过".7m"前的非数字部分
```

**注意**：`%[^0-9]` 跳过所有非数字字符，`%[^0-9.]` 跳过所有非数字和非小数点字符。

---

## 2. `if-else` 省略与执行逻辑

---

### 2.1 `else` 可以省略

```c
if (条件) {
    // 条件成立时执行
}
// else 可以省略，条件不成立时什么都不做
```

---

### 2.2 只有一条语句时可以省略 `{}`

```c
if (条件)
    printf("成立");  // 只有一条语句，可以不加大括号
else
    printf("不成立");
```

**陷阱**：如果有多条语句却忘了加 `{}`，只有第一条属于 `if/else`：

```c
if (条件)
    printf("A");    // 属于 if
    printf("B");    // 不 属于 if！无论条件如何都会执行
```

**建议**：即使是单条语句也加上 `{}`，避免后期添加代码时出错。

---

## 3. 浮点数比较

---

### 3.1 大小比较：直接用 `>` `<` `>=` `<=`

```c
float a = 1.5f, b = 2.0f;
if (a < b) { /* 正确 */ }
```

---

### 3.2 相等比较：必须用误差范围！

```c
#include <math.h>  // fabs()

float a = 0.1f + 0.2f;  // 实际是 0.300000012... 而不是精确 0.3
float b = 0.3f;

// ❌ 错误
if (a == b) { /* 不一定成立 */ }

// ✅ 正确
#define EPSILON 1e-6
if (fabs(a - b) < EPSILON) { /* 成立 */ }
```

**原因**：浮点数在计算机中是近似表示的，直接比较相等几乎总是不可靠。

---

## 4. 循环后加分号——空循环延时

---

### 4.1 基本用法

```c
int i;
for (i = 0; i < 10000; i++);  // 注意这个分号！
// 循环体是空的，CPU 空转 10000 次，起到"延时"效果
```

**💡 关键**：`for (...)` 后面直接跟 `;`，表示循环体为空语句。循环会执行指定次数但什么都不做，相当于让 CPU 忙等（busy-wait），达到短暂延时的目的。

---

### 4.2 常见写法

```c
// 延时一小段时间
for (int i = 0; i < 100000; i++);

// while 版本
int i = 0;
while (i < 100000) i++;
```

---

### 4.3 ⚠️ 陷阱：误加分号

```c
// ❌ 本意是循环打印，但误加了分号
for (i = 0; i < 10; i++);      // 这里循环体是空的！
    printf("%d\n", i);          // 这行只执行一次，打印 10

// ✅ 正确写法
for (i = 0; i < 10; i++)
    printf("%d\n", i);          // 打印 0~9
```

**建议**：写 `for/while` 时注意分号位置。故意用空循环延时时可以加注释说明：

```c
for (i = 0; i < 10000; i++);  // 空循环延时
```

---

## 5. `while` vs `do-while` 与哨兵值

---

### 5.1 核心区别

| 循环 | 特点 | 至少执行一次？ |
|:----|:----|:------------:|
| `do { ... } while (条件);` | 先执行一次，再判断 | ✅ 是 |
| `while (条件) { ... }` | 先判断，再执行 | ❌ 可能一次都不执行 |

---

### 5.2 `do-while` → `while` 转换：哨兵值

想把 `do-while` 改成 `while`，关键在于提前准备一个**能满足条件**的初始值（哨兵值），保证第一次能进循环体。


**外层例子**（控制是否再来一局）：

```c
// do-while 版：先问再判断
char reply;
do {
    // ... 玩游戏 ...
    printf("再来一局？");
    scanf(" %c", &reply);
} while (reply == 'Y' || reply == 'y');
```

```c
// while 版：设哨兵值保证能进
char reply = 'Y';  // 🚩哨兵值：第一次肯定等于 'Y'，保证进循环
while (reply == 'Y' || reply == 'y') {
    // ... 玩游戏 ...
    printf("再来一局？");
    scanf(" %c", &reply);
}
```

**内层例子**（猜数字循环）：

```c
// do-while 版：先猜再判断
int guess;
do {
    printf("请输入数字：");
    scanf("%d", &guess);
} while (guess != image);
```

```c
// while 版：设哨兵值保证能进
int guess = 0;  // 🚩哨兵值：image 范围 1~100，guess=0 肯定不等于，保证进循环
while (guess != image) {
    printf("请输入数字：");
    scanf("%d", &guess);
}
```

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 总结</strong>：<code>do-while</code> 适合「至少要做一次」的场景（猜数至少要猜一次才能判断对不对）。<code>while + 哨兵值</code> 本质是用一个「肯定成立」的初始值骗过条件检查，达到和 <code>do-while</code> 同样的效果。
</div>

---

### 5.3 什么时候用哪个？

| 场景 | 推荐写法 | 原因 |
|:----|:--------:|:----|
| 猜数游戏内层（至少猜一次） | `do-while` 更自然 | 不需要额外设哨兵值 |
| 是否再来一局（外层） | 两者皆可 | 都需要哨兵值或先执行的特性 |
| 文件读取（可能为空文件） | `while` | 文件可能一个数据都没有，不该执行 |
| 菜单选择（至少显示一次） | `do-while` | 菜单总要显示一次让用户选 |

**建议**：不要为了统一而硬改循环类型。`do-while` 的「至少执行一次」本身就是一种语义表达，读代码的人一看就知道「这里肯定要执行至少一次」。强行改成 `while` 反而要多设一个哨兵值，代码没那么直白。

---

## 6. `void` 的两种身份：返回类型 vs 参数列表

**`void` 在 C 里有两个完全不同的用法，别搞混了。**

---

### 6.1 `void` 作为返回类型——不返回值

写在函数名前，表示该函数**没有返回值**：

```c
void say_hello(void) {   // 这个 void 说明不返回任何值
    printf("Hello\n");
    // 不需要写 return，或者写 return; 不带值
}
```

和 `int`/`float` 这类返回类型的对比：

| 写法 | 含义 |
|:----|:----|
| `int add(int a, int b)` | 返回一个整数 |
| `void print_menu(void)` | **不返回任何值** |

---

### 6.2 `void` 在参数列表中——不接受参数

写在括号里，表示该函数**不需要传任何参数**：

```c
int get_time(void) {      // 括号里的 void 说明不需要传参数
    return 12;
}
```

和带参数的对比：

| 写法 | 含义 |
|:----|:----|
| `int add(int a, int b)` | 接收两个 int 参数 |
| `int get_time(void)` | **不接受任何参数** |

---

### 6.3 经典混淆点：`int main(void)`

```c
int  main(void)
↑              ↑
返回int      不接受参数
```

- `int` → 返回整数（告诉操作系统程序正常/异常结束）
- `(void)` → 不需要传参（main 是自动调用的，没人给它传东西）

**对比 `void main(void)`** — 有些教材写这个，但标准 C 里应该写 `int main(void)` 或 `int main(int argc, char *argv[])`。

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 一句话总结</strong>：<code>void</code> 在 <strong>函数名前</strong> = 不返回值；<code>void</code> 在 <strong>括号里</strong> = 不接受参数。两码事，各管各的。
</div>

---

## 7. 函数接口：入口参数、出口参数、返回状态

---

### 7.1 什么是函数接口

**函数接口 = 函数和调用者之间的"契约"**。

接口告诉外面：你该传什么进来，我会吐出什么出去。


💡 **类比：插座**

两孔 / 三孔 = 接口规格，你不用管墙里面怎么走线。

```c
int add(int a, int b);     // ← 这就是接口
// 叫我 add
// 给我两个 int
// 我会还你一个 int
```

---

### 7.2 接口三要素对照表

| 课本术语 | 对应什么 | 说明 |
|:--------|:--------|:----|
| **入口参数** | **形参** | 函数要接收什么数据进来 |
| **出口参数** | **返回值** | 函数算完返回什么结果 |
| **返回状态** | 返回值的一种 | 用返回值表示成功/失败（0=成功, -1=出错） |

---

### 7.3 形参 vs 实参（别搞混了!）

**实参是"进去"的，不是"出来"的。**

| 概念 | 出现时机 | 本质 |
|:----|:--------|:----|
| **形参** | 函数**定义/声明**时写在 `( )` 里的变量 | 占位用的"盒子" |
| **实参** | 函数**调用**时写在 `( )` 里的值 | 往盒子里塞的"东西" |

```c
int max(int a, int b);    // a, b 是形参（入口参数）

int main() {
    int m = max(3, 5);    // 3, 5 是实参（不是出口！）
    //       ↑实参进去   ↑ m 接住返回值（这才是出口参数）
    return 0;             // 返回状态：0 表示正常结束
}
```

---

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 速记</strong><br>
<strong>入口参数 → 形参</strong>（告诉你怎么进去）<br>
<strong>出口参数 → 返回值</strong>（告诉你什么出来）<br>
<strong>返回状态 → 返回值的一种</strong>（表示成功还是失败）<br>
<strong>实参不是出口</strong>——它是你塞给形参的具体值，是"入口"那边的。
</div>

---

## 8. 函数基本原则

---

### 8.1 有返回值时，每个分支都必须有 return

```c
// ❌ 错误：x == 0 时没有 return
int sign(int x) {
    if (x > 0) return 1;
    else if (x < 0) return -1;
    // x == 0 漏了！
}

// ✅ 正确：所有分支都覆盖
int sign(int x) {
    if (x > 0) return 1;
    else if (x < 0) return -1;
    else return 0;  // 兜底
}

// ✅ switch 也要加 default
char grade_to_score(char grade) {
    switch (grade) {
        case 'A': return 90;
        case 'B': return 80;
        case 'C': return 70;
        default: return 0;  // 防意外值
    }
}
```

**⚠️ 坑**：编译只给 warning 不报错，运行时会返回垃圾值（undefined behavior）。能编译 ≠ 代码对。

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 原则</strong>：一个有返回值的函数，走到结尾时必须保证<strong>每条路径都踩到 return</strong>。<code>if-else if</code> 链最后加 <code>else</code> 兜底，<code>switch</code> 加 <code>default</code>。
</div>

---

### 8.2 函数没有返回值，用 `void` 声明

`void` 写在函数名前，表示该函数**不返回任何值**：

```c
void print_hello() {       // void 告诉编译器：我不返回值
    printf("Hello\n");
    // 不需要写 return
}

void early_exit(int x) {   // 也可以写 return; 提前结束
    if (x < 0) {
        printf("无效\n");
        return;            // 提前退出，不带值
    }
    printf("x = %d\n", x);
}
```

**为什么不能省略 `void`？** C 早期有"默认返回 `int`"的规则，不写类型会被当成 `int`：

```c
print_hello() {            // 老 C 默认当它是 int 函数！
    printf("Hello\n");
    // 没有 return → 返回垃圾值
}
```

**`void` 的保护作用**：禁止调用方把返回值当值用：

```c
void say_hello() { ... }

int main() {
    int x = say_hello();   // ❌ 编译错误！void value not ignored
    return 0;
}
```

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 一句话</strong>：<code>void</code> 告诉编译器「这函数干完活就走，不留东西」—— 编译器信了，就不追着你讨 <code>return</code> 了。
</div>

---

## 9. C 程序内存布局（结合微机原理视角）

**打通 C 语言和微机原理：每个变量/函数在内存里到底放哪。**

---

### 9.1 全景图

```
高地址
  ┌──────────────────┐
  │     命令行参数      │  ← argv, envp
  ├──────────────────┤
  │      栈区 ⬇       │  ← 局部变量、函数调用    ← SS:SP
  │    （向下生长）     │
  ├──────────────────┤
  │       堆区 ⬆      │  ← malloc / free
  │    （向上生长）     │
  ├──────────────────┤
  │  .bss（BSS 段）    │  ← 未初始化的全局/静态变量  ← DS
  │                   │     （自动填 0）
  ├──────────────────┤
  │  .data（数据段）    │  ← 有初值的全局/静态变量  ← DS
  ├──────────────────┤
  │  .rodata（只读数据） │  ← 字符串常量 "hello"
  ├──────────────────┤
  │  .text（代码段）    │  ← 函数指令            ← CS
  └──────────────────┘
低地址
```

| 8086 段寄存 | C 中对应 | 存什么 |
|:----------:|:--------|:------|
| `CS` | 代码段 (.text) | 函数指令 |
| `DS` | 数据段 (.data + .bss + .rodata) | 全局/静态变量 |
| `SS` | 栈区 + 堆区 | 局部变量、函数调用、malloc |
| `ES` | 无直接对应 | memcpy 等底层操作偷偷用 |

---

### 9.2 各区域详解

---

#### 9.2.1 代码段 (.text)

存放你写的**函数指令**的机器码，只读（修改就崩）。

```c
void func() { ... }  → 编译成机器码 → 扔进 .text → CS:IP 到这里取指令执行
int main() { ... }   → 也在 .text
```

---

#### 9.2.2 数据段 (.data) — 有初值的全局/静态变量

```c
int a = 10;            // → .data，exe 文件里真存了"10"
static int c = 20;     // → .data
const char* s = "hi";  // → .data（指针 s 本身是全局变量）
```

程序启动时，OS 从 exe 里把初值拷到内存。

---

#### 9.2.3 BSS 段 (.bss) — 没初值（或初值为 0）的全局/静态变量

```c
int b;                // → .bss
static int d;         // → .bss
static int z = 0;     // → .bss（编译器优化：初值是 0 的也扔 BSS，不占 .data 空间）
```

**BSS 的存在意义：省文件空间**

```c
int arr[1000000];          // → BSS
// exe 里只记："BSS 需要 4MB，启动时清零"，文件大小几乎不变
```

**对比：如果扔 .data**

```c
int arr[1000000] = {0};    // → .data
// exe 里实实在在地存了 4MB 的 0，文件膨胀
```

**BSS 为啥要清零？三个原因：**

| 原因 | 说明 |
|:----|:-----|
| **C 标准规定** | 静态存储期的变量没初始化 → 默认是 0 |
| **安全** | 不清零的话，上个程序留下的密码/密钥可能被下个程序读到（信息泄露） |
| **确定性** | 全局变量保证是 0，程序在任何机器上行为一致 |

**那局部变量（栈）为啥不保证清零？——性能考量：**

| | 全局/静态（BSS） | 局部变量（栈） |
|:--|:----------------|:-------------|
| 分配时机 | 程序启动时**一次** | 每次函数调用都可能分配 |
| 清零成本 | 启动时一次性清零，可以接受 | 每次调用都清零 → 频繁调用的函数可能清零几万次 |
| 结果 | **清零**，反正就一次 | **不清零**，让你手动初始化 |

---

#### 9.2.4 只读数据区 (.rodata)

```c
const char* s = "hello";
//                  ↑ "hello" 这个字符串在 .rodata
//       ↑ 指针 s 本身在 .data（s 是全局变量，存着"hello"的地址）
// .rodata 只读，试图改它 → 程序崩
```

---

#### 9.2.5 栈区 — 局部变量 + 函数调用

**和微机原理的栈段是同一个东西。**

```c
void func(int x) {  // x（形参）→ 栈
    int y;          // y（局部变量）→ 栈
    int* p;         // p（局部指针）→ 栈
}                   // MOV SP, BP → 释放，不清零
```

**局部变量出函数后为什么不自动清掉？**

回到硬件层面：释放局部变量就一条指令 `MOV SP, BP` —— CPU 只管把栈顶指针缩回去，**不逐个 POP、不清零**。留下的历史数据成了"垃圾值"，下次别的函数用到同一块栈位置时拿到的就是它。不是真随机，是上一轮残留。

---

#### 9.2.6 堆区 — malloc 出来的内存

```c
int* p = (int*)malloc(4);
// malloc(4) → 堆区拿了 4 字节
// p（局部指针）→ 栈
// 堆和栈反向生长：栈向下，堆向上
```

`malloc` 分配的内存不会自动释放，必须手动 `free`，否则内存泄漏。

---

### 9.3 实例分析

```c
#include <stdio.h>
#include <stdlib.h>

int a = 10;             // ① .data
int b;                  // ② .bss

static int c = 20;      // ③ .data
static int d;           // ④ .bss

const char* s = "hello";// ⑤ s → .data,  "hello" → .rodata

void func(int x) {      // ⑦ func → .text
    int y;              // ⑧ x（形参）, ⑨ y → 栈
    static int z = 0;   // ⑩ .bss
    int* p = (int*)malloc(4); // ⑪ p → 栈, ⑫ malloc → 堆
}
```

| 编号 | 元素 | 所在段 |
|:----:|:----|:-----:|
| ① | `int a = 10;` | .data |
| ② | `int b;` | .bss |
| ③ | `static int c = 20;` | .data |
| ④ | `static int d;` | .bss |
| ⑤ | 指针 `s` 本身 | .data |
| ⑥ | `"hello"` 字符串内容 | .rodata |
| ⑦ | 函数 `func` 的指令 | .text |
| ⑧ | 形参 `x` | 栈 |
| ⑨ | 局部变量 `y` | 栈 |
| ⑩ | `static int z = 0;` | .bss |
| ⑪ | 局部指针 `p` | 栈 |
| ⑫ | `malloc(4)` 分配的内存 | 堆 |

---

### 9.4 BSS 的工作原理详解

---

#### 9.4.1 BSS 怎么记录变量？——只记总数！

BSS **不是逐个记录每个变量的**，而是只记一个总大小：

```c
int b;          // 4 字节
int d;          // 4 字节
int arr[1000000]; // 4,000,000 字节

// 编译器在链接时算好：b(4) + d(4) + arr(4000000) = 4,000,008 字节
// .exe 的节头表里只记一行：
//   .bss  大小: 4000008   偏移: 0（不存在于文件中）
```

**不管有多少个 BSS 变量，.exe 里只占约 16 字节（大小+偏移的记录）。**

- 100 个 BSS 变量 → .exe 额外开销 ≈ 16 字节
- 100 个 .data 变量 → .exe 额外开销 ≈ 16 字节 + 400 字节初值数值

这就是 BSS 省空间的精髓——加载器不需要知道变量名，只需要知道「给我清这么大一块零」。

---

#### 9.4.2 .text 的双重身份——文件中的节 vs 内存中的段

.text 有两副面孔：

| 阶段 | .text 是什么 | 存在哪 |
|:----|:------------|:------|
| 编译完 | **文件里的一个节**（section） | 硬盘上的 .exe 里 |
| 运行时 | **内存里的一块区域**，存着指令 | RAM 里，CS 指向它 |

同理，整个 .exe（PE 格式）包含了 .text、.data、.bss、.rodata 等节。启动时 OS 的加载器按照 PE 格式说明，把这些节搬到内存对应的位置，CPU 就开始执行。

**一句话**：.exe 是**文件格式**，加载到内存后就变成了**内存区域**。

---

#### 9.4.3 .exe 和 .txt 的区别

| 文件类型 | 给谁看的 | 记事本打开 | 内容 |
|:-------|:--------|:---------|:-----|
| .txt | 人 | 正常显示文字 | 纯文本 |
| .c | 人（+ 编译器） | 正常显示代码 | C 语言源码 |
| .exe | **CPU** | **乱码** | 按 PE 格式打包的机器码 + 数据 |

**你写的 .c 源码经过 gcc 编译成 .exe——这就是从「人读」到「CPU 读」的翻译过程。**

---

### 9.5 记忆关键

- **看作用域**：全局 / static → 数据段；局部 → 栈
- **看有无初值**：有显式初值（非 0）→ .data；没初值或初值 = 0 → .bss
- **看是否只读**：字符串常量 → .rodata
- **看是否动态**：malloc → 堆
- **看是否代码**：函数指令 → .text

**堆栈不清零的核心逻辑**：`MOV SP, BP` 一条指令回收，既不清零也不逐个 POP，C 语言把初始化责任交给你，换取了极致的性能。

---

## 10. 全局变量 vs 局部变量

---

### 10.1 判断法则——看花括号

```c
// ⬇⬇⬇ 所有函数外面 → 全局变量 ⬇⬇⬇
int g;                // 全局 → .bss
int h = 10;           // 全局 → .data

void my_func() {
    // 里面 → 局部变量
    int x;            // 局部 → 栈（垃圾值！）
}

int main() {
    // 里面 → 局部变量（跟 my_func 里的 x 地位一样）
    int m;            // 局部 → 栈（垃圾值！）

    // 代码块嵌套
    {
        int n;        // 也是局部，只在这对 {} 里有效
    }

    return 0;
}

// ⬆⬆⬆ 所有函数外面 → 全局变量 ⬆⬆⬆
int k;                // 放在 main 后面也可以！
```

**记住三句话：**

```
在函数外面 → 全局
在函数里面 → 局部
函数地位平等 → main 不特殊
```

---

### 10.2 main 和自定义函数地位一样

`main` 虽然是程序入口，但它**本质上也是函数**。在 main 里面声明的变量，跟在其他函数里声明的一样——局部变量，放栈上。

```c
int g;              // 全局 → BSS，程序启动时自动清 0

int main() {
    int m;          // 局部 → 栈，不初始化就是垃圾值

    printf("g = %d\n", g);  // 0 ✅
    printf("m = %d\n", m);  // 随机值 💀

    return 0;
}
```

| | 全局变量 | main 里的局部变量 |
|:--|:--------|:----------------|
| **声明位置** | 所有函数**外面** | `main()` 的 `{}` **里面** |
| **存储位置** | .data / .bss | **栈** |
| **生命周期** | 程序启动到结束 | main 执行期间 |
| **默认初值** | **0**（BSS 清零） | **垃圾值** |
| **访问范围** | 整个程序所有函数 | 只有 main 里面能访问 |

---

## 11. 变量的一生：int b 从声明到运行的完整流程

以 `int b;`（全局，不初始化）为例，走一遍完整流水线：

| 阶段 | 发生了什么 |
|:----|:----------|
| **① 你写代码** | `int b;` —— 没给初值 |
| **② 编译时** | 编译器识别为「未初始化的全局变量」→ 归入 BSS |
| **③ 链接时** | 链接器计算所有 BSS 变量大小总和：b(4) + ... |
| **④ 生成 .exe** | 节头表只记一行：`.bss 大小 = 总和，文件中不存在` |
| **⑤ 双击运行** | OS 加载器读 .exe，看到 BSS 大小 → 分配内存 → memset 清 0 |
| **⑥ 运行时** | b = 0 ✅ 后续代码可以读写它 |

**关键对比——如果 b 是局部变量：**

| 阶段 | 全局 `int b;`（BSS） | 局部 `int b;`（栈） |
|:----|:-------------------|:-----------------|
| 编译时 | 记录到 BSS，记「大小 4 字节」 | 记「函数进入时 SP -= 4」 |
| 加载时 | **OS 主动清零 → b = 0** | 不管，没这步 |
| 运行时 | 随时访问都是 0（除非你改了） | 从栈上拿来就用 → **垃圾值** |
| 谁负责清 | **系统** | **你自己** |

---

## 12. 四类存储类别

课本上的四种变量类型，对应到内存位置：

---

### 12.1 自动变量（auto）

就是**普通的局部变量**，`auto` 关键字可以省略。

```c
void func() {
    auto int x = 10;  // auto 几乎没人写
    int y = 20;       // 跟上面完全一样，默认就是 auto
}
```

- **存哪**：栈
- **生命周期**：函数执行期间
- **初值**：垃圾值，必须手动初始化

---

### 12.2 静态变量（static）

| 用法 | 含义 | 存哪 |
|:----|:----|:----|
| `static int a;`（函数外） | 只能在本文件访问的全局变量 | .bss / .data |
| `static int b;`（函数内） | 出了函数值还在，下次调用接着用 | .bss / .data |

```c
void counter() {
    static int n = 0;   // 只初始化一次
    n++;
    printf("%d ", n);
}

int main() {
    counter();  // 1
    counter();  // 2  ← 值保留下来了！
    counter();  // 3
}
```

**static 把变量从栈上挪到了数据段，生命周期变成全程。**

---

### 12.3 外部变量（extern）

声明「这个变量在别的文件里定义」，用来跨文件共享全局变量：

```c
// file1.c
int global = 10;

// file2.c
extern int global;    // 告诉编译器：global 在其他 .c 文件里
void func() {
    printf("%d", global);  // 10
}
```

- 存哪：.data / .bss（就是普通全局变量）
- `extern` 只是声明，不是定义

---

### 12.4 寄存器变量（register）

建议编译器把变量放到 **CPU 寄存器**里，而不是内存里：

```c
void func() {
    register int i;             // 建议放寄存器
    for (i = 0; i < 10000; i++) {
        // 频繁访问 i → 放寄存器比放内存快
    }
}
```

- **只是一个建议**，编译器可以忽略
- **不能取地址**（`&i` 会报错）——寄存器没有内存地址
- 现代编译器优化很强，基本自动搞定，现在用得少了

---

### 12.5 四类速查表

| 类型 | 关键字 | 存哪 | 活多久 | 默认初值 |
|:----|:------|:----|:------|:--------|
| 自动变量 | `auto`（默认省略） | **栈** | 函数执行期间 | 垃圾值 |
| 静态变量 | `static` | **数据段** | 程序全程 | 0 |
| 外部变量 | `extern`（声明） | **数据段** | 程序全程 | 0 |
| 寄存器变量 | `register` | **寄存器**（不一定） | 函数执行期间 | 垃圾值 |

---

## 13. 递归与函数调用栈

递归每次调用自己，都在栈上压一层新帧——每层帧里存放该次调用的局部变量和返回地址。

---

### 13.1 阶乘的栈行为

```c
int fact(int n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}
```

调用 `fact(4)` 时栈的变化：

```
fact(4) 调用：                 fact(4) 返回：
  [栈顶]                        [栈顶]
  ┌──────────┐                  ┌──────────┐
  │ fact(4)  │ ← n=4           │ (已释放)  │
  ├──────────┤                  ├──────────┤
  │ fact(3)  │ ← n=3           │ fact(3)  │ ← n=3
  ├──────────┤                  ├──────────┤
  │ fact(2)  │ ← n=2           │ fact(2)  │ ← n=2
  ├──────────┤                  ├──────────┤
  │ fact(1)  │ ← n=1, 触底!    │ fact(1)  │ ← 返回 1
  └──────────┘                  └──────────┘
```

**触底反弹**：`fact(1)` 返回 1 → 释放栈帧 → `fact(2)` 拿结果 2×1 → 释放 → `fact(3)` 拿结果 3×2 → 释放 → `fact(4)` 拿结果 4×6 = 24

每一层入栈时 CPU 执行 `PUSH` + `SUB SP`（开辟局部变量空间），出栈时 `MOV SP, BP` + `POP` + `RET`——和你今天学的**局部变量释放机制**完全一致。

---

### 13.2 栈溢出

如果递归太深（没有出口、或者层数太多），栈会被撑爆：

```c
void infinite() {
    int x;          // 每次调用都占栈
    infinite();     // 永远不返回 → 栈空间耗尽 → 崩溃 💥
}
```

```
栈不断往下长：
  ┌──────────┐
  │ infinite │
  ├──────────┤
  │ infinite │
  ├──────────┤
  │ infinite │
  ├──────────┤
  │    ...    │
  ├──────────┤
  │  ⚡栈溢出！ │ ← 栈空间用完了
  └──────────┘
```

这就联系到微机原理里 SS:SP 的管理——**栈空间是有限的**，SP 不能无限减下去。

---

### 13.3 斐波那契的递归栈

```c
int fib(int n) {
    if (n <= 1) return n;
    return fib(n-1) + fib(n-2);
}
```

`fib(4)` 的调用树：

```
            fib(4)
           /      \
      fib(3)     fib(2)
      /    \      /   \
   fib(2) fib(1) fib(1) fib(0)
   /    \
fib(1) fib(0)
```

每次调用都会在栈上开辟新帧，返回时释放——跟阶乘原理一样，但分支更多，栈会反复压入弹出。

---

## 14. 堆栈清零哲学

为什么全局变量自动清零，而局部变量不清零？这背后是 C 语言的**效率取舍**：

| | 全局/静态（BSS） | 局部变量（栈） |
|:--|:----------------|:-------------|
| 分配时机 | 程序启动时**一次** | 每次函数调用都可能分配 |
| 清零成本 | 启动时一次性清零，可以接受 | 每次调用都清零 → 频繁调用的函数可能清零几万次 |
| 结果 | **清零**，反正就一次 | **不清零**，让你手动初始化 |

**CPU 层面的原因**：释放局部变量就一条指令 `MOV SP, BP`——CPU 只管把栈顶指针缩回去，不逐个 POP、不清零。留下的历史数据成了「垃圾值」，下次别的函数用到同一块栈位置时拿到的就是它。**不是真随机，是上一轮残留。**

**BSS 清零的三个原因：**

| 原因 | 说明 |
|:----|:-----|
| **C 标准规定** | 静态存储期的变量没初始化 → 默认是 0 |
| **安全** | 不清零的话，上个程序留下的密码/密钥可能被下个程序读到（信息泄露） |
| **确定性** | 全局变量保证是 0，程序在任何机器上行为一致 |

**一句话总结 C 语言的取舍哲学**：频繁分配的（栈）→ 交给你自己初始化；一次分配的（BSS）→ 系统帮你清好。要性能，也要安全。

---

## 15. 宏（Macro）

---

### 15.1 宏的本质

**宏不是变量，不是函数，是编译前的文字替换规则。** 预处理器看到 `#define` 定义的名字，之后遇到这个名字就直接把后面的文字"贴"上去。

| | 宏 `#define PI 3.14` | `const double pi = 3.14;` |
|:-|:--------------------|:------------------------|
| 什么时候处理？ | 编译前（预处理阶段） | 编译时 |
| 占不占内存？ | ❌ 不占，替换成字面值 | ✅ 占 8 字节 |
| 能不能改？ | ❌ 不能改（本质是替换规则） | ✅ 可以 |
| 有没有类型？ | ❌ 没有，就是一段文本 | ✅ 有类型，编译器能检查 |

---

### 15.2 对象宏（Object-like Macro）——替代常量值

```c
#define PI      3.14159
#define MAX     100
#define NAME    "小溪"
```

预处理后：

```c
// 你写：
float area = PI * r * r;

// 预处理后变成：
float area = 3.14159 * r * r;
```

**对号入座**：课本上讲的"宏常量"就是对象宏。

---

### 15.3 函数宏（Function-like Macro）——替代简单运算

```c
#define SQUARE(x)  x * x
#define MAX(a, b)  a > b ? a : b

// 使用：
int y = SQUARE(5);      // → int y = 5 * 5;
int m = MAX(3, 7);      // → int m = 3 > 7 ? 3 : 7;
```

**它不是函数调用！** 只是文本替换，没有参数传递、没有栈帧、没有返回值。

---

### 15.4 ⚠️ 函数宏的陷阱

**陷阱 1：缺括号导致优先级错误**

```c
#define SQUARE(x)  x * x

int y = SQUARE(2 + 3);      // 期望 5*5=25

// 实际展开：int y = 2 + 3 * 2 + 3;  → 11 ❌
```

**修复**：宏体内每个参数和整个表达式都加括号

```c
#define SQUARE(x)  ((x) * (x))
```

**陷阱 2：参数有副作用**

```c
#define MAX(a, b)  ((a) > (b) ? (a) : (b))

int x = 1, y = 2;
int m = MAX(x++, y++);

// 展开成：((x++) > (y++) ? (x++) : (y++))
// x++ 被执行两次！后果不可预料 ❌
```

**规则：宏的参数里永远不要放 `++`、`--`、函数调用等有副作用的表达式。**

**陷阱 3：多行需要用 `\` 续行**

```c
#define PRINT_SUM(a, b)  \
    printf("sum = %d\n", (a) + (b)); \
    printf("done\n");
```

反斜杠后面不能有空格。

---

### 15.5 函数宏 vs 真正的函数

| | 宏 | 函数 |
|:-|:--|:----|
| 调用开销 | **无**，编译前已替换 | 有，压栈传参、跳转返回 |
| 类型检查 | ❌ 没有类型概念 | ✅ 编译器检查类型 |
| 副作用风险 | ⚠️ 容易踩坑 | ✅ 安全的求值规则 |
| 代码膨胀 | 每次调用复制一份 → 体积变大 | 只有一份代码 |
| 多行逻辑 | ❌ 用 `\` 续行很丑 | ✅ 正常写函数体 |

---

### 15.6 `#undef`——取消宏定义

```c
#define TEMP 100
// ... 用 TEMP 的地方都被替换成 100 ...

#undef TEMP      // 取消定义
// 从这行开始 TEMP 不再是宏了

int TEMP = 50;   // ✅ 可以当普通变量名用了
```

---

### 15.7 内置宏（预定义好的宏）

```c
printf("文件：%s\n", __FILE__);    // 当前文件名
printf("行号：%d\n", __LINE__);    // 当前行号
printf("日期：%s\n", __DATE__);    // 编译日期
printf("时间：%s\n", __TIME__);    // 编译时间
```

调试打印时很好用。

---

### 15.8 实战建议（现在就用得上）

| 你要做什么 | 推荐 |
|:----------|:-----|
| 定义常量 | 用 `const`，有类型检查更安全 |
| 条件编译（`#if`）需要值 | 用 `#define` |
| 头文件保护 | 用 `#ifndef / #define / #endif` |
| 短小的运算替代 | 小宏可以，注意括号**拉满** |
| 复杂逻辑 | 写普通函数 |

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 一句话总结</strong>：宏在编译前干活，干的是找字换字的体力活。常量用 <code>const</code>、保护宏背模板、函数宏先别用——等以后熟悉了再回来补括号和副作用的坑。</div>

---

## 16. 条件编译与头文件保护

---

### 16.1 条件编译指令一览

| 指令 | 含义 |
|:----|:-----|
| `#if` | 表达式为真（非零）时保留代码 |
| `#ifdef` | 宏已定义时保留代码 |
| `#ifndef` | 宏未定义时保留代码 |
| `#elif` | else if 的组合 |
| `#else` | 否则 |
| `#endif` | **结束（必须配对）** |
| `#define` | 定义宏 |
| `#undef` | 取消宏定义 |

---

### 16.2 四种组合用法

**组合 ①：`#if` / `#elif` / `#else` / `#endif`**

```c
#define DEBUG_LEVEL 2

#if DEBUG_LEVEL >= 3
    printf("详细调试信息\n");
#elif DEBUG_LEVEL >= 2
    printf("中等调试信息\n");
#else
    printf("无调试\n");
#endif
```

后面接的是**表达式**（常量、算术、逻辑运算都行），类似 if-else if-else 链。

**组合 ②：`#ifdef` / `#else` / `#endif`**

```c
#define WINDOWS

#ifdef WINDOWS
    printf("Windows版本\n");
#else
    printf("其他平台\n");
#endif
```

只关心宏**有没有被定义**，不关心值是多少。`#define WINDOWS` 和 `#define WINDOWS 1` 效果一样。

**组合 ③：`#ifndef` / `#endif`（头文件保护）**

```c
#ifndef MY_HEADER_H
#define MY_HEADER_H

// 头文件内容...

#endif
```

**组合 ④：`#if defined()` / `#if !defined()`**

```c
#define A
// #define B

#if defined(A) && !defined(B)
    printf("A定义了，B没定义\n");
#endif
```

`defined()` 是个运算符，可以配合 `&&` `||` `!` 做组合判断。`#ifdef` 是它的语法糖，但只能判断单个宏。

**一张图记忆：**

```
判断"值"表达式    →  #if / #elif / #else / #endif
判断"有没有定义"  →  #ifdef / #ifndef / #else / #endif
灵活的"定义判断"  →  #if defined() / #if !defined()
```

**注意：** `#ifdef` 后面不能写 `#elif`，想链式判断定义要用 `#if defined()` + `#elif defined()`。

---

### 16.3 头文件保护（Header Guard / Include Guard）

**保护的是什么？** 保护**头文件本身的内容不被重复包含进同一个 .c 文件**。

**为什么要保护？**

```c
// student.h 没有保护
struct Student {
    int id;
    char name[20];
};

// main.c
#include "student.h"
#include "score.h"      // score.h 里也 #include "student.h"
// → struct Student 被定义了两次 → 编译报错！
```

预处理器只是机械地把头文件内容粘贴进来，不管重不重复。不加保护，同一个结构体在一个 .c 文件里被定义了两次就会报错。

**⚠️ 注意**：单条函数声明（只有分号的那种）可以重复，但**结构体定义、函数定义、变量定义**都不行。所以头文件保护是通用的——不管里面写什么，保证整个文件只生效一次。

**保护的逻辑（文字版）：**

```
第一次 #include：
  #ifndef TOOLS_H → TOOLS_H 不在宏列表 → 条件为真
  → #define TOOLS_H → 把 TOOLS_H 加入宏列表
  → 放行头文件内容
  → #endif 结束

第二次 #include：
  #ifndef TOOLS_H → TOOLS_H 已在宏列表 → 条件为假
  → 跳过所有内容直到 #endif
  → 什么都没输出
```

**保护宏的写法（模板）：**

```c
// myheader.h
#ifndef MYHEADER_H    // ① 检查：这个宏定义了没？
#define MYHEADER_H    // ② 没定义 → 定义它，留下"已来过"的标记

// 头文件正常内容…
// 函数声明、结构体、宏定义……

#endif                // ③ 结束
```

| 行 | 作用 | 比喻 |
|:--|:-----|:-----|
| `#ifndef XXX` | 检查门锁 | 看"XX"在不在名单上 |
| `#define XXX` | 上锁 | 在名单上写下"XX" |
| `#endif` | 关门 | 条件编译块结束 |

**保护宏的名字惯例：** 头文件名全大写，点变下划线

| 头文件 | 保护宏名 |
|:------|:---------|
| `math_utils.h` | `MATH_UTILS_H` |
| `student_info.h` | `STUDENT_INFO_H` |

**关键理解：**

- ✅ 保护宏**写在头文件内部**，不是写在 `#include` 周围
- ✅ 每个头文件自己管好自己的保护，使用者只管 `#include`
- ❌ `.c` 文件不需要写保护宏
- ❌ 保护宏名字不要和别的头文件撞了

**`#pragma once` 替代方案：**

```c
#pragma once
int square(int x);
```

一行搞定，但这是**编译器扩展**（非标准 C），而 `#ifndef` 是 C 标准规定的，所有编译器都支持。

---

### 16.4 小项目的文件组织

**推荐思路：先跑通，再拆文件。**

> **先写对，再拆开。**

学习方法：

```
第 1 步：所有代码写在一个 .c 文件里，跑通
第 2 步：把函数声明复制出来，粘到 .h 文件加保护宏
第 3 步：把函数实现剪到单独的 .c 文件里
第 4 步：改编译命令，把所有 .c 文件一起编译
```

**小项目的标准结构：**

```
project/
├── tools.h       ← 所有函数声明 + 保护宏
├── main.c        ← main() 入口
├── input.c       ← 输入相关功能
├── calc.c        ← 计算相关功能
└── output.c      ← 输出相关功能
```

**每个 `.c` 文件都要 `#include "tools.h"`**，因为：
- `main.c`：需要知道函数长什么样才能调用
- 其他 `.c` 文件：需要知道函数声明，以及可能相互调用

**编译命令：**
```bash
gcc main.c input.c calc.c output.c -o program
```

**什么时候一开始就拆文件？** 出现以下信号时：
1. 代码超过 300~400 行，一个文件翻来翻去很麻烦
2. 有多个独立的功能模块
3. 想复用某个模块到下一个项目

<div style="background:#FFF3CD; border-left:4px solid #FFA500; padding:10px; margin:12px 0;">
<strong>💡 一句话总结</strong>：先让代码跑起来，再考虑怎么拆。拆文件是个独立步骤，写完再收拾。</div>

---

## 17. 函数返回值与数组传参

**核心问题：函数怎么"带东西回来"？`return` 一次只能带一个值，那一堆数据怎么办？**

---

### 17.1 return / break / continue 三兄弟

| 语句 | 效果 | 函数后面的代码还跑吗 |
|:----|:----|:----|
| `return 值` | **整个函数**立即结束，值带回调用处 | ❌ 不跑 |
| `break` | 只跳出**当前循环** | ✅ 继续跑 |
| `continue` | 只跳过**本次迭代**剩余部分 | ✅ 循环继续下一圈 |

```c
int FindFirstZero(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        if (arr[i] == 0)
        {
            return i;      // 整个函数在这里结束，后面代码全不执行
        }
        printf("i=%d 不是0\n", i);
    }
    return -1;   // 循环正常走完没找到，才走到这里
}
```

---

### 17.2 return 只能带一个值，数组不能作为返回值

**函数不能返回数组！** 语法上不允许：

```c
int[] CalcTotal(...)   // ❌ 错误！数组不能作为返回类型
```

`return` 后面只能跟一个值（int / long / float / char ……）。

---

### 17.3 数组参数 = 传地址 = 共享数据

**数组传参传的是地址**，函数里操作的就是 main 里那份数组，改完就留在里面了。

> 💡 类比：`return` 是寄快递（一次一件）；数组参数是共享仓库（函数直接在里面改，改完都在）。

```c
// 函数把总分算进 total[]，main 里的 TotalScore[] 跟着变
void CalcTotal(int chinese[], int math[], int english[], int total[], int x)
{
    int i;
    for (i = 0; i < x; i++)
    {
        total[i] = chinese[i] + math[i] + english[i];
    }
}
```

**自己代码里的证据**：`ReadScore(long ID[], int score[])` 没有把数据 return 回来，但 main 里数组全是数据——就是靠数组参数共享的。

---

### 17.4 二维数组作函数参数：列数必须写

```c
void PrintTotal(long ID[], int Score[][3], int n)
//                           ↑↑↑ 列数必须写
//                 行数可以空着，n 传进来
```

**为什么？** 二维数组在内存里是一排排连续存放的，编译器要知道"一行多长"才能算出第 i 行的地址。列数不给，编译器算不出行地址。

---

### 17.5 输出所有学生的总分

**场景：每个学生一行（二维数组），列是各科成绩。直接在 main 里算。**

```c
for (i = 0; i < N; i++)              // i = 第几个学生（行）
{
    Sum = 0;
    for (j = 0; j < COURSE; j++)     // j = 哪一科（列）
    {
        Sum += StudentScore[i][j];   // 累加这一行
    }
    printf("学号%ld：总分%d\n", StudentId[i], Sum);
}
```

**什么时候值得封装成函数？**

- 这段逻辑要在别的地方再用（排序、求平均都要算总分）
- main 越来越长（超过一屏）

只用一次的话，循环里直接写最清晰，不用硬造函数。

---

### 17.6 ⚠️ 平均分整除坑

```c
average[i] = total[i] / 3;      // ❌ 整数除法！250/3 = 83，小数丢掉
average[i] = total[i] / 3.0;    // ✅ 浮点除法！250/3.0 = 83.33
```

`int / int` 结果是 int；想让结果是小数，除号两边至少一个是浮点数。

---

### 17.7 传值 vs 传地址：为什么数组改了 main 就变

**普通变量传的是"复印件"，数组传的是"仓库地址"。**

```c
void TryChange(int x)          // 普通变量：传值（复印件）
{
    x = 100;                   // 改复印件，main 的 n 不变
}

void TryChangeArr(int arr[])   // 数组：传地址（仓库钥匙）
{
    arr[0] = 100;              // 改共享内存，main 的 a[0] 变
}
```

| 传参类型 | 传什么 | 函数里改，main 变吗 |
|:----|:----|:----|
| `int x` 普通变量 | 值（复印件） | ❌ 不变 |
| `int arr[]` 数组 | 地址（仓库钥匙） | ✅ 变 |

---

### 17.8 数组形参 = 指针

**函数形参里，数组写法就是指针写法**：

| 形参写法 | 等价于 | 含义 |
|:----|:----|:----|
| `long studentid[]` | `long *studentid` | 指向 long 的指针 |
| `int studentscore[][3]` | `int (*studentscore)[3]` | 指向"含 3 个 int 的一行"的指针 |

所以 `studentscore[i][j]` 底层就是：

```c
地址 = 首地址 + i × 3 × 4 + j × 4
      ↑首地址   ↑跳了i行   ↑跳了j列
```

指针里存的是 main 数组的**首地址**，下标运算 = 顺着地址找内存格子，读写的就是 main 那份。

**⚠️ 经典坑**：二维数组形参**不等于** `int **`（指向指针的指针）！

```c
int ReadScore(int **studentscore, ...)   // ❌ 这是二维指针，不是二维数组！
```

`int **` 是"指向指针的指针"，和"指向一行的指针"是两码事。

---

### 记忆关键

- `return` 结束**整个函数**，`break` 只跳出**循环**，`continue` 跳过**本次迭代**
- 函数**不能返回数组**，靠数组参数传地址共享数据
- 二维数组传参**列数必须写**，行数可不写
- 整数除法丢小数：`/ 3` 和 `/ 3.0` 不一样
- 普通变量传值（复印件），数组传地址（共享仓库）
- 数组形参 = 指针：`int a[]` ≡ `int *a`；`int a[][3]` ≡ `int (*a)[3]`
